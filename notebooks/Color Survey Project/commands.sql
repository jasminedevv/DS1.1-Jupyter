/* get the user IDs of all women. */

SELECT id
FROM users
WHERE ychrom = 0

/* count the number of xy respondents */

/* 
1. look in the table of users 
2. get the ids of the ones that are male
3. get the colors those users submitted
4. return a list of those colors
*/